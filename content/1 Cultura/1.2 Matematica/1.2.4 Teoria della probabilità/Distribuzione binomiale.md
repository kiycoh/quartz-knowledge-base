---
tags:
  - calcolo-probabilità
  - distribuzione-binomiale
  - statistica-inferenziale
---

# Definizione di distribuzione binomiale
La distribuzione binomiale è un concetto fondamentale nel calcolo delle probabilità, utilizzato per descrivere il numero di successi in una serie di prove indipendenti, ciascuna con due possibili esiti: successo o insuccesso.

La funzione di probabilità della distribuzione binomiale è data dalla formula: 
$$P(X=k)=(kn​)p^k(1−p)^{n−k}$$
La distribuzione binomiale è definita da due parametri:
- $n$: il numero totale di prove.
- $p$: la probabilità di successo in ciascuna prova.

Una variabile aleatoria $X$ che segue una distribuzione binomiale si indica come $X∼B(n,p)$. Questa variabile può assumere valori interi da $0$ a $n$, rappresentando il numero di successi ottenuti nelle $n$ prove

### Esempi applicativi
Funzione utilizzata ad esempio dalle polizze o per calcolare quante giocate un giocare d'azzardo può fare prima di perdere tutto il capitale.

$$P(c)=P(x \geq 2) = P(X=$$